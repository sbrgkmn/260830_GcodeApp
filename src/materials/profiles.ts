import type { MaterialProfile } from '../types/domain'

export const PLA_PROFILE: MaterialProfile = {
  id: 'pla-generic',
  name: 'PLA · Generic',
  density: 1.24,
  nozzleTemperature: 215,
  bedTemperature: 60,
  maxVolumetricFlow: 12,
}
